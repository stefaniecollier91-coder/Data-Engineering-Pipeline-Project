# Update these values before running the Dash app.
# Example local PostgreSQL connection string:
POSTGRES_URL=postgresql+psycopg2://postgres:your_password@localhost:5432/cvg_flight_weather

# Optional: set to true only when you need to demo layout without a database.
ALLOW_SAMPLE_FALLBACK=false
